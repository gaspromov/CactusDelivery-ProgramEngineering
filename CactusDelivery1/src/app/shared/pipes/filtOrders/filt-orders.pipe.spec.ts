import { FiltOrdersPipe } from './filt-orders.pipe';

describe('FiltOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
